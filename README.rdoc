= Active View

Active View is a framework for handling view template lookup and rendering, and
provides view helpers that assist when building HTML forms, Atom feeds in the
context of an Acive Model. The view layer is then free to instantiate other view
models for rendering.


actions/models/[resource]/{show,form}.rb
actions/presenters/[resource]/form.rb
actions/views/[resource]/{show,form}.html.erb
